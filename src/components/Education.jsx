import React from 'react';
import SectionTitle from './SectionTitle';

function Education() {
	return (
        <div className="py-12">
            <SectionTitle>Education</SectionTitle>
            <div id="education" className="flex items-center justify-center flex-col py-20">

            </div>
        </div>
	);
}

export default Education;